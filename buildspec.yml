version: 0.2

phases:
  install:
    commands:
      - echo "Installing required packages..."
      - go get -u github.com/stretchr/testify
  pre_build:
    commands:
      - echo "Running tests..."
      - go test -coverprofile=coverage.out
  build:
    commands:
      - echo "Logging in to Amazon ECR..."
      - aws ecr get-login-password --region $AWS_DEFAULT_REGION | docker login --username AWS --password-stdin 559816800025.dkr.ecr.$AWS_DEFAULT_REGION.amazonaws.com
      - echo "Building the Docker image..."
      - docker build -t my-golang-app .
      - echo "Tagging the Docker image..."
      - docker tag my-golang-app:latest 559816800025.dkr.ecr.$AWS_DEFAULT_REGION.amazonaws.com/my-golang-app:latest
      - echo "Pushing the Docker image..."
      - docker push 559816800025.dkr.ecr.$AWS_DEFAULT_REGION.amazonaws.com/my-golang-app:latest

artifacts:
  files:
    - Dockerfile
    - coverage.out

reports:
  test-coverage: # Test coverage report
    file-format: COBERTURAXML
    files:
      - coverage.out
    base-directory: '.'